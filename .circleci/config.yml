version: 2
jobs:
  one:
    docker:
      - image: circleci/node:8
    step:
      - checkout
      - run: echo "a first hello"
      - run: sleep 25
  two:
    docker:
      - image: circleci/node:12
    branches:
        only:
          - master  
    steps:
      - checkout
      - run: echo "Hello World!"
      - run: yarn install
      - run: yarn build

workflows:
  version: 2
  one_and_two:
    jobs:
      - one
      - two
